#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
# shellcheck source=_ruby-env.sh
source "$(dirname "$0")/_ruby-env.sh"

if [[ ! -d vendor/bundle ]]; then
  echo "Dependencies not installed. Run ./bin/setup first." >&2
  exit 1
fi

exec bundle exec jekyll serve --livereload "$@"
